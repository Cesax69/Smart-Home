# Configuración de la Base de Datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tasks_db
DB_USER=postgres
DB_PASSWORD=change-me
DB_SCHEMA=public

# Configuración del Servidor
PORT=3002
NODE_ENV=development

# URL del API Gateway (para operaciones remotas como borrar archivos en Drive)
# Si no se establece, se usará http://localhost:3000 por defecto
API_GATEWAY_URL=http://localhost:3000

# Configuración de Logs
LOG_LEVEL=info